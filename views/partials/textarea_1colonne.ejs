<div class="question textarea_1colonne">
  <label for="<%= question.id + (multiples ? "-1" : "") %>"><%= question.nom %></label>
  <textarea id="<%= question.id + (multiples ? "-1" : "") %>" name="<%= question.id + (multiples ? "-1" : "") %>"></textarea>
</div>
